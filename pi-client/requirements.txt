# HTTP 客户端
requests==2.32.3

# CUPS 打印接口 (树莓派 Linux 环境)
pycups==2.0.4

# 配置管理
python-dotenv==1.0.1

# 指数退避重试
tenacity==9.0.0

# PDF 页面缩放 (写入前将 PDF 页面物理调整为目标纸张)
pypdf==5.1.0

# ── 可选依赖 (通过 install-deps.sh --all 自动安装) ──
# pygame==2.6.1          # TTS 蜂鸣回退 / 音频播放
# numpy==2.2.0           # pygame 音频合成
# edge-tts==6.1.16       # Microsoft Edge TTS (在线高质量)

# 测试
pytest==8.3.4
